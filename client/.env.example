# API Configuration
# API URL will be auto-detected based on current domain
# For production (Vercel): automatically uses current domain + /api
# For local development: uses localhost:5000

# No manual configuration needed!
